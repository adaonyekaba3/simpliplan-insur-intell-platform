# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Option 1: Local PostgreSQL
# DATABASE_URL="postgresql://user:password@localhost:5432/simpliplan?schema=public"

# Option 2: Vercel Postgres (recommended for deployment)
# Get from: https://vercel.com/dashboard -> Storage -> Postgres
# DATABASE_URL="postgres://default:xxxxx@xxxx.postgres.vercel-storage.com:5432/verceldb"

# Option 3: Supabase (free tier available)
# Get from: https://supabase.com/dashboard -> Project Settings -> Database
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Option 4: Neon (free tier available)
# Get from: https://console.neon.tech -> Connection String
DATABASE_URL="postgresql://user:password@localhost:5432/simpliplan?schema=public"

# =============================================================================
# NEXTAUTH.JS CONFIGURATION
# =============================================================================
# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-generate-with-openssl-rand-base64-32"

# Development URL
NEXTAUTH_URL="http://localhost:3000"

# Production: Update to your deployed URL
# NEXTAUTH_URL="https://your-app.vercel.app"

# =============================================================================
# GOOGLE OAUTH CREDENTIALS
# =============================================================================
# How to get Google OAuth credentials:
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Navigate to: APIs & Services > Credentials
# 4. Click "Create Credentials" > "OAuth 2.0 Client IDs"
# 5. Configure OAuth consent screen if not done
# 6. Application type: Web application
# 7. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (for development)
#    - https://your-app.vercel.app/api/auth/callback/google (for production)
# 8. Copy Client ID and Client Secret below

GOOGLE_CLIENT_ID="your-google-client-id-here"
GOOGLE_CLIENT_SECRET="your-google-client-secret-here"

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# How to get Stripe credentials:
# 1. Sign up at: https://stripe.com
# 2. Go to: https://dashboard.stripe.com/test/apikeys
# 3. Copy "Publishable key" and "Secret key" for test mode
# 4. For webhook secret:
#    a. Go to: https://dashboard.stripe.com/test/webhooks
#    b. Click "Add endpoint"
#    c. Endpoint URL: http://localhost:3000/api/stripe/webhook (for local testing)
#       OR use Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
#    d. Select events: checkout.session.completed
#    e. Copy the "Signing secret"

# Test mode keys (start with sk_test_ and pk_test_)
STRIPE_SECRET_KEY="sk_test_your_secret_key_here"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"

# Production keys (when ready to go live)
# STRIPE_SECRET_KEY="sk_live_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
